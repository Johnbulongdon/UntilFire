<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme: no action bar, dark background -->
    <style name="Theme.UntilFire" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorBackground</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen theme shown before the TWA loads -->
    <style name="Theme.UntilFire.Splash" parent="Theme.UntilFire">
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>

</resources>
