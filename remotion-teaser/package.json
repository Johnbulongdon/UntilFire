{
  "name": "untilfire-teaser",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "studio": "remotion studio src/index.ts",
    "render:vertical": "remotion render src/index.ts PersonalFirePlanTeaser out/untilfire-personal-fire-plan.mp4 --codec=h264 --crf=18 --pixel-format=yuv420p",
    "still:hook": "remotion still src/index.ts PersonalFirePlanTeaser out/still-000-hook.png --frame=0",
    "still:bank": "remotion still src/index.ts PersonalFirePlanTeaser out/still-090-bank.png --frame=90",
    "still:investments": "remotion still src/index.ts PersonalFirePlanTeaser out/still-180-investments.png --frame=180",
    "still:benchmarks": "remotion still src/index.ts PersonalFirePlanTeaser out/still-230-benchmarks.png --frame=230",
    "still:insights": "remotion still src/index.ts PersonalFirePlanTeaser out/still-300-insights.png --frame=300",
    "still:timeline": "remotion still src/index.ts PersonalFirePlanTeaser out/still-370-timeline.png --frame=370",
    "still:cta": "remotion still src/index.ts PersonalFirePlanTeaser out/still-450-cta.png --frame=450",
    "stills": "npm run still:hook && npm run still:bank && npm run still:investments && npm run still:benchmarks && npm run still:insights && npm run still:timeline && npm run still:cta",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.290",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "remotion": "^4.0.290",
    "simple-icons": "^16.21.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
